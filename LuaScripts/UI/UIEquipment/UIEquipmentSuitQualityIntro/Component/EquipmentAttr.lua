---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by 1.
--- DateTime: 2023/3/6 18:37
---


local EquipmentAttr = BaseClass('EquipmentAttr', UIBaseContainer)
local base = UIBaseContainer

local name_path = "effect_name"
local value_path = "effect_value"
local lock_path = "effect_lock"

local function OnCreate(self)
    base.OnCreate(self)
    self:ComponentDefine()
end

local function OnDestroy(self)
    self:ComponentDestroy()
    base.OnDestroy(self)
end

local function ComponentDefine(self)
    self.name = self:AddComponent(UITextMeshProUGUIEx, name_path)
    self.value = self:AddComponent(UITextMeshProUGUIEx, value_path)
    self.lock = self:AddComponent(UITextMeshProUGUIEx, lock_path)
end

local function ComponentDestroy(self)

end

local function SetData(self, data)
    self.data = data
    self.name:SetText(self.data.desc)
    if self.data.isLocked then
        self.lock:SetText(self.data.value)
        self.value:SetText("")
    else
        self.lock:SetText("")
        self.value:SetText(self.data.value)
    end
end

EquipmentAttr.OnCreate= OnCreate
EquipmentAttr.OnDestroy = OnDestroy
EquipmentAttr.ComponentDefine = ComponentDefine
EquipmentAttr.ComponentDestroy = ComponentDestroy
EquipmentAttr.SetData = SetData

return EquipmentAttr