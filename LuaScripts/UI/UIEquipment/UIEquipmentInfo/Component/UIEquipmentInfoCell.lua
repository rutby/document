---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by 1.
--- DateTime: 2023/2/28 18:25
---

local UIEquipmentInfoCell = BaseClass('UIEquipmentInfoCell', UIBaseContainer)
local base = UIBaseContainer

local effect_path = "Effect"
local desc_text = "Effect/EffectDesc"
local value_text = "Effect/Effect_Value"

local unlock_path = "Unlock"
local unlock_desc_text = "Unlock/UnlockDesc"
local unlock_value_text = "Unlock/Unlock_Value"

local function OnCreate(self)
    base.OnCreate(self)
    self:ComponentDefine()
end

local function OnDestroy(self)
    self:ComponentDestroy()
    base.OnDestroy(self)
end

local function ComponentDefine(self)
    self.effect = self:AddComponent(UIBaseContainer, effect_path)
    self.desc = self:AddComponent(UITextMeshProUGUIEx, desc_text)
    self.value = self:AddComponent(UITextMeshProUGUIEx, value_text)

    self.unlock = self:AddComponent(UIBaseContainer, unlock_path)
    self.unlock_desc = self:AddComponent(UITextMeshProUGUIEx, unlock_desc_text)
    self.unlock_value = self:AddComponent(UITextMeshProUGUIEx, unlock_value_text)
end

local function ComponentDestroy(self)

end

local function SetData(self, data)
    self.data = data
    self.effect:SetActive(false)
    self.unlock:SetActive(false)
    if self.data.isLocked then
        self.unlock:SetActive(true)
        self.unlock_desc:SetText(self.data.desc)
        self.unlock_value:SetText(self.data.value)
    else
        self.effect:SetActive(true)
        self.desc:SetText(self.data.desc)
        self.value:SetText(self.data.value)
    end
end

UIEquipmentInfoCell.OnCreate= OnCreate
UIEquipmentInfoCell.OnDestroy = OnDestroy
UIEquipmentInfoCell.ComponentDefine = ComponentDefine
UIEquipmentInfoCell.ComponentDestroy = ComponentDestroy
UIEquipmentInfoCell.SetData = SetData

return UIEquipmentInfoCell