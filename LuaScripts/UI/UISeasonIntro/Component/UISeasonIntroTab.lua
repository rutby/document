---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Beef.
--- DateTime: 2023/2/1 20:55
---

local UISeasonIntroTab = BaseClass("UISeasonIntroTab", UIBaseContainer)
local base = UIBaseContainer

local off_path = "Off"
local off_text_path = "Off/OffText"
local on_path = "On"
local on_text_path = "On/OnText"

local function OnCreate(self)
    base.OnCreate(self)
    self:DataDefine()
    self:ComponentDefine()
end

local function OnDestroy(self)
    self:ComponentDestroy()
    self:DataDestroy()
    base.OnDestroy(self)
end

local function ComponentDefine(self)
    self.off_btn = self:AddComponent(UIButton, off_path)
    self.off_btn:SetOnClick(function()
        self:OnClick()
    end)
    self.off_text = self:AddComponent(UIText, off_text_path)
    self.on_btn = self:AddComponent(UIButton, on_path)
    self.on_btn:SetOnClick(function()
        self:OnClick()
    end)
    self.on_text = self:AddComponent(UIText, on_text_path)
end

local function ComponentDestroy(self)
    self.off_btn = nil
    self.off_text = nil
    self.on_btn = nil
    self.on_text = nil
end

local function DataDefine(self)
    self.index = 0
    self.onClick = nil
    self.selected = false
end

local function DataDestroy(self)
    self.index = nil
    self.onClick = nil
    self.selected = nil
end

local function OnEnable(self)
    base.OnEnable(self)
end

local function OnDisable(self)
    base.OnDisable(self)
end

local function Refresh(self)
    self.on_btn:SetActive(self.selected)
    self.off_btn:SetActive(not self.selected)
end

local function SetName(self, name)
    self.off_text:SetLocalText(name)
    self.on_text:SetLocalText(name)
end

local function SetIndex(self, index)
    self.index = index
    self:Refresh()
end

local function SetOnClick(self, onClick)
    self.onClick = onClick
end

local function SetSelected(self, selected)
    self.selected = selected
    self:Refresh()
end

local function OnClick(self)
    if not self.selected then
        if self.onClick then
            self.onClick()
        end
    end
end

UISeasonIntroTab.OnCreate= OnCreate
UISeasonIntroTab.OnDestroy = OnDestroy
UISeasonIntroTab.ComponentDefine = ComponentDefine
UISeasonIntroTab.ComponentDestroy = ComponentDestroy
UISeasonIntroTab.DataDefine = DataDefine
UISeasonIntroTab.DataDestroy = DataDestroy
UISeasonIntroTab.OnEnable = OnEnable
UISeasonIntroTab.OnDisable = OnDisable

UISeasonIntroTab.Refresh = Refresh
UISeasonIntroTab.SetName = SetName
UISeasonIntroTab.SetIndex = SetIndex
UISeasonIntroTab.SetOnClick = SetOnClick
UISeasonIntroTab.SetSelected = SetSelected
UISeasonIntroTab.OnClick = OnClick

return UISeasonIntroTab