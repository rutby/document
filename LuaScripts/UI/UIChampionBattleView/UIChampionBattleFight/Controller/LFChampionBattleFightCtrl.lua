---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by 1.
--- DateTime: 2022/2/14 14:19
---
local LFChampionBattleFightCtrl = BaseClass("LFChampionBattleFightCtrl", UIBaseCtrl)

local function CloseSelf(self)
    UIManager:GetInstance():DestroyWindow(UIWindowNames.LFChampionBattleFight)
end

local function Close(self)
    UIManager:GetInstance():DestroyWindowByLayer(UILayer.Normal)
end

local function ShowPlayerInfo(self, userUid)
    if string.IsNullOrEmpty(userUid) then
        return ""
    end
    if userUid == LuaEntry.Player.uid then
        UIManager:GetInstance():OpenWindow(UIWindowNames.UIPlayerInfo, userUid)
    else
        UIManager:GetInstance():OpenWindow(UIWindowNames.UIOtherPlayerInfo, userUid)
    end
end

local function SetHeadImg(self, userHead, guluFrame, userUid, pic, picvec, headSkinId, headSkinET)
    userHead:SetData(userUid, pic, picvec)
    local headBgImg = DataCenter.DecorationDataManager:GetHeadFrame(headSkinId, headSkinET, guluFrame == 1)
    guluFrame:SetActive(headBgImg ~= nil)
    if headBgImg ~= nil then
        guluFrame:LoadSprite(headBgImg)
    end
end

LFChampionBattleFightCtrl.CloseSelf = CloseSelf
LFChampionBattleFightCtrl.Close = Close
LFChampionBattleFightCtrl.SetHeadImg = SetHeadImg
LFChampionBattleFightCtrl.ShowPlayerInfo = ShowPlayerInfo

return LFChampionBattleFightCtrl