---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by 1.
--- DateTime: 2022/3/8 20:45
---

local UIChampionBattleResultHintView = BaseClass("UIChampionBattleResultHintView", UIBaseView)
local base = UIBaseView
local Localization = CS.GameEntry.Localization

local closeBtn_path = "UICommonMidPopUpTitle/bg_mid/CloseBtn"
local maskBtn_path = "UICommonMidPopUpTitle/panel"
local title_path = "UICommonMidPopUpTitle/bg_mid/titleText"
local total_num_text_path = "total_num_text"
local win_title_path = "win_node/win_title"
local win_text_path = "win_node/win_text"

local lose_title_path = "lose_node/lose_title"
local lose_text_path = "lose_node/lose_text"

local score_title_path = "score_node/score_title"
local score_text_path = "score_node/score_text"

local function OnCreate(self)
    base.OnCreate(self)
    self:ComponentDefine()
    self:DataDefine()
    self:RefreshView()
end

local function OnDestroy(self)
    self:ComponentDestroy()
    self:DataDestroy()
    base.OnDestroy(self)
end

local function OnEnable(self)
    base.OnEnable(self)
end

local function OnDisable(self)
    base.OnDisable(self)
end

local function ComponentDefine(self)
    self.closeBtnN = self:AddComponent(UIButton, closeBtn_path)
    self.closeBtnN:SetOnClick(function()
        self.ctrl:CloseSelf()
    end)
    self.maskBtnN = self:AddComponent(UIButton, maskBtn_path)
    self.maskBtnN:SetOnClick(function()
        self.ctrl:CloseSelf()
    end)
    self.titleTxtN = self:AddComponent(UITextMeshProUGUIEx, title_path)
    self.titleTxtN:SetLocalText(302120)
    self.total_num_text = self:AddComponent(UIText, total_num_text_path)
    self.win_title = self:AddComponent(UIText, win_title_path)
    self.win_title:SetLocalText(302122)
    self.win_text = self:AddComponent(UIText, win_text_path)

    self.lose_title = self:AddComponent(UIText, lose_title_path)
    self.lose_text = self:AddComponent(UIText, lose_text_path)
    self.lose_title:SetLocalText(302123)

    self.score_title = self:AddComponent(UIText, score_title_path)
    self.score_text = self:AddComponent(UIText, score_text_path)
    self.score_title:SetLocalText(302124)
end

local function ComponentDestroy(self)
    self.closeBtnN = nil
    self.titleTxtN = nil
    self.contentN = nil
    self.total_num_text = nil
    self.win_title = nil
    self.win_text = nil

    self.lose_title = nil
    self.lose_text = nil

    self.score_title = nil
    self.score_text = nil

end

local function DataDefine(self)
    
end

local function DataDestroy(self)

end

local function RefreshView(self)
    local data = self.ctrl:GetShowData()
    if data ~= nil then
        self.total_num_text:SetLocalText(302121, data.total)
        self.win_text:SetText(tostring(data.win))
        self.lose_text:SetText(tostring(data.lose))
        self.score_text:SetText(tostring(data.score))
    end
end

UIChampionBattleResultHintView.OnCreate = OnCreate
UIChampionBattleResultHintView.OnDestroy = OnDestroy
UIChampionBattleResultHintView.OnEnable = OnEnable
UIChampionBattleResultHintView.OnDisable = OnDisable
UIChampionBattleResultHintView.ComponentDefine = ComponentDefine
UIChampionBattleResultHintView.ComponentDestroy = ComponentDestroy
UIChampionBattleResultHintView.DataDefine = DataDefine
UIChampionBattleResultHintView.DataDestroy = DataDestroy
UIChampionBattleResultHintView.RefreshView = RefreshView

return UIChampionBattleResultHintView