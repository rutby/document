---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Imaginaerum.
--- DateTime: 17/1/2024 上午11:41
---
local QueueTitleItem = BaseClass("QueueTitleItem", UIBaseContainer)
local base = UIBaseContainer
local Localization = CS.GameEntry.Localization
local ListType = {
    Build = 1,
    Army = 2,
    Science = 3,
    Troop = 4,
}
local name_txt_path = "name_text"
local function OnCreate(self)
    base.OnCreate(self)
    self:ComponentDefine()
    self:DataDefine()
end

local function OnDestroy(self)
    self:ComponentDestroy()
    self:DataDestroy()
    base.OnDestroy(self)
end

local function OnEnable(self)
    base.OnEnable(self)
end

local function OnDisable(self)
    base.OnDisable(self)
end

local function ComponentDefine(self)
    self.name_text = self:AddComponent(UITextMeshProUGUIEx, name_txt_path)
end

local function ComponentDestroy(self)

end

local function DataDefine(self)

end

local function DataDestroy(self)

end

local function OnAddListener(self)
    base.OnAddListener(self)
end

local function OnRemoveListener(self)
    base.OnRemoveListener(self)
end

local function SetData(self,type)
    if type == ListType.Build then
        self.name_text:SetLocalText(470000)
    elseif type == ListType.Army then
        self.name_text:SetLocalText(470002)
    elseif type == ListType.Science then
        self.name_text:SetLocalText(470001)
    elseif type == ListType.Troop then
        self.name_text:SetLocalText(302254)
    end
end

QueueTitleItem.OnCreate = OnCreate
QueueTitleItem.OnDestroy = OnDestroy
QueueTitleItem.OnEnable = OnEnable
QueueTitleItem.OnDisable = OnDisable
QueueTitleItem.ComponentDefine = ComponentDefine
QueueTitleItem.ComponentDestroy = ComponentDestroy
QueueTitleItem.DataDefine = DataDefine
QueueTitleItem.DataDestroy = DataDestroy
QueueTitleItem.OnAddListener = OnAddListener
QueueTitleItem.OnRemoveListener = OnRemoveListener

QueueTitleItem.SetData = SetData

return QueueTitleItem