---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by 1.
--- DateTime: 2021/8/24 16:58
---
local UIActMTRewardCell = BaseClass("UIActMTRewardCell", UIBaseContainer)
local base = UIBaseContainer
local UICommonItem = require "UI.UICommonItem.UICommonItem"

local titleTxt_path = "rewardBg/TitleBg/Title"
local content_path = "rewardBg/Rewards"
local selfRankBg_path = "rewardBg/selfRankBg"


local function OnCreate(self)
    base.OnCreate(self)

    self.rewardModels ={}
    self.rewardItemsList = {}
    
    self.titleN = self:AddComponent(UITextMeshProUGUIEx, titleTxt_path)
    self.contentN = self:AddComponent(UIBaseContainer, content_path)
    self.selfRankBgN = self:AddComponent(UIBaseContainer, selfRankBg_path)
end

local function OnDestroy(self)
    self.rewardModels = nil
    self.rewardItemsList = nil
    
    self.titleN = nil
    self.contentN = nil
    base.OnDestroy(self)
end


local function ShowRewards(self,param,isSelf)
    self.titleN:SetLocalText(300665,param.title)
    self.selfRankBgN:SetActive(isSelf)
    self:SetAllRewardsDestroy()
    self.rewardModelCount =0
    local list = param.reward
    if list~=nil and #list>0 then
        for i = 1, table.length(list) do
            --复制基础prefab，每次循环创建一次
            self.rewardModelCount= self.rewardModelCount+1
            self.rewardModels[self.rewardModelCount] = self:GameObjectInstantiateAsync(UIAssets.UICommonItemSize, function(request)
                if request.isError then
                    return
                end
                local go = request.gameObject;
                go.gameObject:SetActive(true)
                go.transform:SetParent(self.contentN.transform)
                go.transform:Set_localScale(ResetScale.x, ResetScale.y, ResetScale.z)
                -- go.transform.localScale = Vector3.New(0.54, 0.54, 1)
                local nameStr = tostring(NameCount)
                go.name = nameStr
                NameCount = NameCount + 1
                local cell = self.contentN:AddComponent(UICommonItem,nameStr)
                cell:ReInit(list[i])
                table.insert(self.rewardItemsList,cell)
            end)
        end
    end
end


local function SetAllRewardsDestroy(self)
    self.contentN:RemoveComponents(UICommonItem)
    if self.rewardModels~=nil then
        for k,v in pairs(self.rewardModels) do
            if v ~= nil then
                self:GameObjectDestroy(v)
            end
        end
    end
    self.rewardModels ={}
    self.rewardItemsList = {}
end


UIActMTRewardCell.OnCreate = OnCreate
UIActMTRewardCell.OnDestroy = OnDestroy


UIActMTRewardCell.ShowRewards = ShowRewards
UIActMTRewardCell.SetAllRewardsDestroy = SetAllRewardsDestroy

return UIActMTRewardCell