---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Beef.
--- DateTime: 2022/1/19 20:14
---

local UIHeroStationTip = BaseClass("UIHeroStationTip", UIBaseContainer)
local base = UIBaseContainer

local this_path = ""
local close_path = "TipClose"
local desc_path = "Bg/TipDesc"

local function OnCreate(self)
    base.OnCreate(self)
    self:DataDefine()
    self:ComponentDefine()
end

local function OnDestroy(self)
    self:ComponentDestroy()
    self:DataDestroy()
    base.OnDestroy(self)
end

local function ComponentDefine(self)
    self.anim = self:AddComponent(UIAnimator, this_path)
    self.canvas_group = self:AddComponent(UICanvasGroup, this_path)
    self.close_btn = self:AddComponent(UIButton, close_path)
    self.close_btn:SetOnClick(function() self:Hide() end)
    self.desc_text = self:AddComponent(UIText, desc_path)
end

local function ComponentDestroy(self)
    self.anim = nil
    self.canvas_group = nil
    self.close_btn = nil
    self.desc_text = nil
end

local function DataDefine(self)
    self.show = false
end

local function DataDestroy(self)
    self.show = nil
end

local function OnEnable(self)
    base.OnEnable(self)
end

local function OnDisable(self)
    base.OnDisable(self)
end

local function Show(self)
    if self.show then
        return
    end
    self.show = true
    self.anim:Play("CommonPopup_movein", 0, 0)
    self.close_btn:SetActive(true)
end

local function Hide(self)
    if not self.show then
        return
    end
    self.show = false
    self.anim:Play("CommonPopup_moveout", 0, 0)
    self.close_btn:SetActive(false)
end

local function SetDesc(self, desc)
    self.desc_text:SetText(desc)
    CS.UnityEngine.UI.LayoutRebuilder.ForceRebuildLayoutImmediate(self.desc_text.transform)
    CS.UnityEngine.UI.LayoutRebuilder.ForceRebuildLayoutImmediate(self.anim.transform)
end

UIHeroStationTip.OnCreate= OnCreate
UIHeroStationTip.OnDestroy = OnDestroy
UIHeroStationTip.ComponentDefine = ComponentDefine
UIHeroStationTip.ComponentDestroy = ComponentDestroy
UIHeroStationTip.DataDefine = DataDefine
UIHeroStationTip.DataDestroy = DataDestroy
UIHeroStationTip.OnEnable = OnEnable
UIHeroStationTip.OnDisable = OnDisable

UIHeroStationTip.Show = Show
UIHeroStationTip.Hide = Hide
UIHeroStationTip.SetDesc = SetDesc

return UIHeroStationTip