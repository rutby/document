---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Beef.
--- DateTime: 2022/6/9 12:15
---

local HeroEffectSkillData = BaseClass("HeroEffectSkillData")

local function __init(self)
    -- key
    self.effectId = 0
    
    -- config
    self.skillId = 0
    self.heroId = 0
    self.costNum = 0
    
    -- server data
    self.num = 0 -- 已使用数量
    self.time = 0 -- 上次重置时间用来判断跨天
end

local function __delete(self)
    self.effectId = nil
    self.skillId = nil
    self.heroId = nil
    self.costNum = nil
    self.num = nil
    self.time = nil
end

local function InitConfig(self, effectId, skillId, heroId, costNum)
    self.effectId = effectId
    self.skillId = skillId
    self.heroId = heroId
    self.costNum = costNum
end

local function SetServerData(self, serverData)
    self.num = serverData.num
    self.time = serverData.time
end

HeroEffectSkillData.__init = __init
HeroEffectSkillData.__delete = __delete

HeroEffectSkillData.InitConfig = InitConfig
HeroEffectSkillData.SetServerData = SetServerData

return HeroEffectSkillData
