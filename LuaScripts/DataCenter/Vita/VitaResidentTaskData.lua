---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Beef.
--- DateTime: 2024/4/10 16:39
---

local VitaResidentTaskData = BaseClass("VitaResidentTaskData")

local function __init(self)
    self.uuid = 0
    self.id = 0
    self.count = 0
    self.rUuid = 0
end

local function ParseServerData(self, serverData)
    if serverData.uuid then
        self.uuid = serverData.uuid
    end
    if serverData.taskId then
        self.id = serverData.taskId
    end
    if serverData.count then
        self.count = serverData.count
    end
    if serverData.rUuid then
        self.rUuid = serverData.rUuid
    end
end

local function CanFinish(self)
    local line = LocalController:instance():getLine(TableName.ResidentStrike, self.id)
    return self.count >= line.para2
end

VitaResidentTaskData.__init = __init

VitaResidentTaskData.ParseServerData = ParseServerData
VitaResidentTaskData.CanFinish = CanFinish

return VitaResidentTaskData