---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Beef.
--- DateTime: 2023/2/28 15:40
---

local GloryVsData = BaseClass("GloryVsData")

local function __init(self)
    self.allianceId = ""
    self.serverId = 0
    self.alName = ""
    self.alIcon = ""
    self.abbr = ""
    self.isWin = 0
    self.alScore = 0
    self.changeRank = 0
    self.isAtk = 0
    self.allianceSeasonScore = 0
    self.fightMember = 0
    self.occupy = 0
    self.crashwb = 0
    self.crashtc = 0
    self.placeAssemble = 0
    self.createtc = 0
end

local function ParseServerData(self, serverData)
    if serverData.allianceId then
        self.allianceId = serverData.allianceId
    end
    if serverData.serverId then
        self.serverId = serverData.serverId
    end
    if serverData.alName then
        self.alName = serverData.alName
    end
    if serverData.alIcon then
        self.alIcon = serverData.alIcon
    end
    if serverData.abbr then
        self.abbr = serverData.abbr
    end
    if serverData.isWin then
        self.isWin = serverData.isWin
    end
    if serverData.alScore then
        self.alScore = serverData.alScore
    end
    if serverData.changeRank then
        self.changeRank = math.abs(serverData.changeRank)
    end
    if serverData.isAtk then
        self.isAtk = serverData.isAtk
    end
    if serverData.allianceSeasonScore then
        self.allianceSeasonScore = serverData.allianceSeasonScore
    end
    if serverData.fightMember then
        self.fightMember = serverData.fightMember
    end
    if serverData.occupy then
        self.occupy = serverData.occupy
    end
    if serverData.crashwb then
        self.crashwb = serverData.crashwb
    end
    if serverData.crashtc then
        self.crashtc = serverData.crashtc
    end
    if serverData.placeAssemble then
        self.placeAssemble = serverData.placeAssemble
    end
    if serverData.createtc then
        self.createtc = serverData.createtc
    end
end

GloryVsData.__init = __init

GloryVsData.ParseServerData = ParseServerData

return GloryVsData