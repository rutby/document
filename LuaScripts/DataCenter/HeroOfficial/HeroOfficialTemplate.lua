---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Beef.
--- DateTime: 2022/7/26 18:27
---

local HeroOfficialTemplate = BaseClass("HeroOfficialTemplate")

local function __init(self)
    self.id = 0
    self.camp = 0
    self.type = 0
    self.pos = 0
    self.name = 0
    self.unlockLv = 0
    self.effect = 0
    self.maxVal = 0
    self.maxHeroPower = 0
    self.extraCondition = { 0, 0 }
    self.extraVal = 0
    self.buffLv = 0
end

local function __delete(self)
    self.id = nil
    self.camp = nil
    self.type = nil
    self.pos = nil
    self.name = nil
    self.unlockLv = nil
    self.effect = nil
    self.maxVal = nil
    self.maxHeroPower = nil
    self.extraCondition = nil
    self.extraVal = nil
    self.buffLv = nil
end

local function InitData(self,row)
    if row == nil then
        return
    end

    self.id = tonumber(row:getValue("id")) or 0
    self.camp = tonumber(row:getValue("camp")) or 0
    self.type = tonumber(row:getValue("type")) or 0
    self.pos = tonumber(row:getValue("official_position")) or 0
    self.name = tonumber(row:getValue("official_name")) or 0
    self.unlockLv = tonumber(row:getValue("unlock_lv")) or 0
    self.effect = tonumber(row:getValue("effect")) or 0
    self.maxVal = tonumber(row:getValue("max_value")) or 0
    self.maxHeroPower = tonumber(row:getValue("max_hero_power")) or 0
    self.extraCondition = { 0, 0 }
    local extraConditionStr = row:getValue("extra_condition") or ""
    if extraConditionStr ~= "" then
        local spls = string.split(extraConditionStr, ";")
        if #spls == 2 then
            self.extraCondition[1] = tonumber(spls[1])
            self.extraCondition[2] = tonumber(spls[2])
        end
    end
    self.extraVal = tonumber(row:getValue("extra_value")) or 0
    self.buffLv = tonumber(row:getValue("buff_lv")) or 0
end

local function GetValType(self)
    return GetTableData(TableName.EffectNumDesc, self.effect, "type")
end

HeroOfficialTemplate.__init = __init
HeroOfficialTemplate.__delete = __delete
HeroOfficialTemplate.InitData = InitData
HeroOfficialTemplate.GetValType = GetValType

return HeroOfficialTemplate