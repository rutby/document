---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Beef.
--- DateTime: 2023/3/23 11:10
---

local CrossWormData = BaseClass("CrossWormData")

local function __init(self)
    self.usedTimes = 0 -- 今日已使用的补兵次数 crossFillTimes
    self.usedTimesRefreshTime = 0 -- 今日已使用的补兵次数刷新时间
    self.usedFreeTimes = 0 -- 今日已使用的免费补兵次数 hasFree，已弃用
end

local function ParseServerData(self, serverData)
    if serverData.crossFillTimes then
        self.usedTimes = serverData.crossFillTimes
        self.usedTimesRefreshTime = UITimeManager:GetInstance():GetServerTime()
    end
    if serverData.costFree then
        self.usedFreeTimes = serverData.hasFree
    end
end

CrossWormData.__init = __init
CrossWormData.__delete = __delete

CrossWormData.ParseServerData = ParseServerData

return CrossWormData