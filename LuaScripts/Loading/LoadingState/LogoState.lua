---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Imaginaerum.
--- DateTime: 1/3/2024 下午5:09
---
local LoadingStateBase = require("Loading.LoadingState.LoadingStateBase")
local LogoState = BaseClass("LogoState", LoadingStateBase)
local Const = require("Loading.Const")

function LogoState:__init(startupLoading)
    LoadingStateBase:__init(startupLoading)
end

function LogoState:OnEnter(args)
    self._animTime = 0
    self._logoAnimLength = -1
end

function LogoState:OnExit()

end

function LogoState:OnUpdate()
    if self._startupLoading.UILoading ~= nil then
        if self._logoAnimLength < 0 then
            self._logoAnimLength = self._startupLoading.UILoading:GetLogoAnimLength()
        end

        self._animTime = self._animTime + Time.deltaTime
        if self._animTime > self._logoAnimLength then
            self._startupLoading:SetState(Const.LoadingState.Permission)
        end
    end
end

return LogoState