---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by 
--- DateTime: 
---
local DesertTalentPageNameCheckMessage = BaseClass("DesertTalentPageNameCheckMessage", SFSBaseMessage)
local base = SFSBaseMessage
local Localization = CS.GameEntry.Localization

local function OnCreate(self,name)
    base.OnCreate(self)
    self.sfsObj:PutUtfString("name",name)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    local errCode =  t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode) 
    else
        EventManager:GetInstance():Broadcast(EventId.PageNameCheckEvent)
    end
end

DesertTalentPageNameCheckMessage.OnCreate = OnCreate
DesertTalentPageNameCheckMessage.HandleMessage = HandleMessage

return DesertTalentPageNameCheckMessage