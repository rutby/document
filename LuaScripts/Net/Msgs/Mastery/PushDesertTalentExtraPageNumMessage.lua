---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by 
--- DateTime: 
---
local PushDesertTalentExtraPageNumMessage = BaseClass("PushDesertTalentExtraPageNumMessage", SFSBaseMessage)
local base = SFSBaseMessage
local Localization = CS.GameEntry.Localization

local function OnCreate(self)
    base.OnCreate(self)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    local errCode =  t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode) 
    else
        DataCenter.MasteryManager:HandleExtraPageNum(t)
    end
end

PushDesertTalentExtraPageNumMessage.OnCreate = OnCreate
PushDesertTalentExtraPageNumMessage.HandleMessage = HandleMessage

return PushDesertTalentExtraPageNumMessage