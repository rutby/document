---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by zzl.
--- DateTime: 
--- 重置其他页天赋

local ResetDesertTalentOtherPageMessage = BaseClass("ResetDesertTalentOtherPageMessage", SFSBaseMessage)
local base = SFSBaseMessage

local function OnCreate(self, page)
    base.OnCreate(self)
    self.sfsObj:PutInt("page",page)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    if t["errorCode"] ~= nil then
        UIUtil.ShowTipsId(t["errorCode"])
        return
    end
    
    DataCenter.MasteryManager:OnReset(t)
end

ResetDesertTalentOtherPageMessage.OnCreate = OnCreate
ResetDesertTalentOtherPageMessage.HandleMessage = HandleMessage

return ResetDesertTalentOtherPageMessage