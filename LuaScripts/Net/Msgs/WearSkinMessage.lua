---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by 1.
--- DateTime: 2022/12/8 15:25
---
local WearSkinMessage = BaseClass("WearSkinMessage", SFSBaseMessage)
local base = SFSBaseMessage

local function OnCreate(self, param)
    base.OnCreate(self)
    if param ~= nil then
        self.sfsObj:PutInt("skinId", param.skinId)
        if param.formationUuid ~= nil and param.formationUuid ~= 0 then
            self.sfsObj:PutLong("formationUuid", param.formationUuid)
        end
    end
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    DataCenter.DecorationDataManager:WearSkinHandler(t)
end

WearSkinMessage.OnCreate = OnCreate
WearSkinMessage.HandleMessage = HandleMessage

return WearSkinMessage