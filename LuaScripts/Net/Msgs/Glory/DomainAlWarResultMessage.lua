---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Beef.
--- DateTime: 2023/2/28 16:28
---

local DomainAlWarResultMessage = BaseClass("DomainAlWarResultMessage", SFSBaseMessage)
local base = SFSBaseMessage

local function OnCreate(self, page)
    base.OnCreate(self)
    self.sfsObj:PutInt("page", page)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    if t["errorCode"] ~= nil then
        UIUtil.ShowTipsId(t["errorCode"])
        return
    end

    DataCenter.GloryManager:HandleGetHistory(t)
end

DomainAlWarResultMessage.OnCreate = OnCreate
DomainAlWarResultMessage.HandleMessage = HandleMessage

return DomainAlWarResultMessage