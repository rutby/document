---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by 
--- DateTime: 
---
local ReadNoticeMessage = BaseClass("ReadNoticeMessage", SFSBaseMessage)
local base = SFSBaseMessage

local function OnCreate(self,uuid)
    base.OnCreate(self)
    self.sfsObj:PutLong("uuid",uuid)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    local errCode =  t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode)
    else
        DataCenter.WorldNoticeManager:ReadNoticeHandle(t)
    end
end

ReadNoticeMessage.OnCreate = OnCreate
ReadNoticeMessage.HandleMessage = HandleMessage

return ReadNoticeMessage