---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by 
--- DateTime: 
---
local ReceiceNoticeRewardMessage = BaseClass("ReceiceNoticeRewardMessage", SFSBaseMessage)
local base = SFSBaseMessage

local function OnCreate(self,uuid)
    base.OnCreate(self)
    self.sfsObj:PutLong("uuid",uuid)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    local errCode =  t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode)
    else
        DataCenter.WorldNoticeManager:ReceiveRewardHandle(t)
    end
end

ReceiceNoticeRewardMessage.OnCreate = OnCreate
ReceiceNoticeRewardMessage.HandleMessage = HandleMessage

return ReceiceNoticeRewardMessage