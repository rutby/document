---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by admin.
--- DateTime: 2024/4/28 14:53
---

local AutoFinishDetectEventMessage = BaseClass("AutoFinishDetectEventMessage", SFSBaseMessage)
local base = SFSBaseMessage
local Localization = CS.GameEntry.Localization

local function OnCreate(self, uuids)
    base.OnCreate(self)
    
    local arr = SFSArray.New()
    table.walk(uuids,function (k,v)
        arr:AddLong(v)
    end)
    self.sfsObj:PutSFSArray("uuidArr", arr)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)

    local errCode =  t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode)
    else

    end
end

AutoFinishDetectEventMessage.OnCreate = OnCreate
AutoFinishDetectEventMessage.HandleMessage = HandleMessage

return AutoFinishDetectEventMessage