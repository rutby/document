---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by 1.
--- DateTime: 2022/5/7 14:40
---
local ResetDetectEventMessage = BaseClass("ResetDetectEventMessage", SFSBaseMessage)
local base = SFSBaseMessage
local Localization = CS.GameEntry.Localization

local function OnCreate(self, uuid)
    base.OnCreate(self)
    self.sfsObj:PutLong("uuid", uuid)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)

    local errCode =  t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode)
    else
        DataCenter.RadarCenterDataManager:HandleResetData(t)
        EventManager:GetInstance():Broadcast(EventId.GetAllDetectInfo)
    end
end

ResetDetectEventMessage.OnCreate = OnCreate
ResetDetectEventMessage.HandleMessage = HandleMessage

return ResetDetectEventMessage