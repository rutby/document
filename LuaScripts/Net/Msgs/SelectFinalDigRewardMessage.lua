---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by 1.
--- DateTime: 2022/10/20 18:08
---SelectFinalDigRewardMessage

local SelectFinalDigRewardMessage = BaseClass("SelectFinalDigRewardMessage", SFSBaseMessage)
local base = SFSBaseMessage
local Localization = CS.GameEntry.Localization

local function OnCreate(self, activityId, bigRewardIndex)
    base.OnCreate(self)
    self.sfsObj:PutInt("activityId", activityId)
    self.sfsObj:PutInt("bigRewardIndex", bigRewardIndex)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    local errCode =  t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode)
    else
        DataCenter.DigActivityManager:OnRecvSelectFinalRewardSucc(t)
    end
end

SelectFinalDigRewardMessage.OnCreate = OnCreate
SelectFinalDigRewardMessage.HandleMessage = HandleMessage

return SelectFinalDigRewardMessage