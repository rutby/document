---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Imaginaerum.
--- DateTime: 2022/10/11 15:12
---
local WorldGetMarchInfosMessage = BaseClass("WorldGetMarchInfosMessage", SFSBaseMessage)
local base = SFSBaseMessage
local Localization = CS.GameEntry.Localization

local function OnCreate(self,x,y, needCross)
    base.OnCreate(self)
    self.sfsObj:PutInt("x",x)
    self.sfsObj:PutInt("y",y)
    self.sfsObj:PutInt("worldId", LuaEntry.Player:GetCurWorldId())
    if needCross then
        self.sfsObj:PutBool("needCross", needCross)
    end
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    local errCode =  t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode)
    else
        DataCenter.WorldMarchDataManager:WorldMarchInfoGetHandle(t)
    end
end

WorldGetMarchInfosMessage.OnCreate = OnCreate
WorldGetMarchInfosMessage.HandleMessage = HandleMessage

return WorldGetMarchInfosMessage