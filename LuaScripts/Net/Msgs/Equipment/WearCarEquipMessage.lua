---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by 1.
--- DateTime: 2023/3/1 20:03
---

local WearCarEquipMessage = BaseClass("WearCarEquipMessage", SFSBaseMessage)
local base = SFSBaseMessage

local function OnCreate(self, carIndex, type, uuid)
    base.OnCreate(self)
    self.sfsObj:PutInt("carIndex", carIndex)
    self.sfsObj:PutInt("type", type)
    self.sfsObj:PutLong("uuid", uuid)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    DataCenter.EquipmentDataManager:WearCarEquipHandler(t)
end

WearCarEquipMessage.OnCreate = OnCreate
WearCarEquipMessage.HandleMessage = HandleMessage

return WearCarEquipMessage