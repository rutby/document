---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by 1.
--- DateTime: 2023/3/1 20:03
---

local CarEquipExpMessage = BaseClass("CarEquipExpMessage", SFSBaseMessage)
local base = SFSBaseMessage
local Localization = CS.GameEntry.Localization

local function OnCreate(self, goodsId, num, uuid)
    base.OnCreate(self)
    self.sfsObj:PutUtfString("goodsId", tostring(goodsId))
    self.sfsObj:PutInt("num", num)
    self.sfsObj:PutLong("uuid", uuid)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    DataCenter.EquipmentDataManager:EquipmentAddExpHandler(t)
end

CarEquipExpMessage.OnCreate = OnCreate
CarEquipExpMessage.HandleMessage = HandleMessage

return CarEquipExpMessage