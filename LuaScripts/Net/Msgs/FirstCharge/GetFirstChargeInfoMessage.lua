---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by zzl.
--- DateTime: 
---获取首充信息

local GetFirstChargeInfoMessage = BaseClass("GetFirstChargeInfoMessage", SFSBaseMessage)
local base = SFSBaseMessage

local function OnCreate(self)
    base.OnCreate(self)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    local errCode =  t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode)
    else
        DataCenter.ActFirstChargeData:HandleCharge(t)
    end
end

GetFirstChargeInfoMessage.OnCreate = OnCreate
GetFirstChargeInfoMessage.HandleMessage = HandleMessage

return GetFirstChargeInfoMessage