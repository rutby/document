---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by 1020.
--- DateTime: 2023/5/24 14:51
---

local GetMiningCarRewardInfoMessage = BaseClass("GetMiningCarRewardInfoMessage", SFSBaseMessage)
local base = SFSBaseMessage
local Localization = CS.GameEntry.Localization

local function OnCreate(self, activityId, queueId, index)
    base.OnCreate(self)
    self.sfsObj:PutInt("activityId", tostring(activityId))
    self.sfsObj:PutInt("queueId", tostring(queueId))
    self.sfsObj:PutInt("index", tostring(index))
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    local errCode =  t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode)
    else
        DataCenter.MiningManager:OnRecvMiningCarRewardInfo(t)
    end
end

GetMiningCarRewardInfoMessage.OnCreate = OnCreate
GetMiningCarRewardInfoMessage.HandleMessage = HandleMessage

return GetMiningCarRewardInfoMessage