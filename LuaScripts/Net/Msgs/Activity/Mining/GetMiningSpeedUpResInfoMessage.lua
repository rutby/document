---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by 1020.
--- DateTime: 2023/5/26 12:00
---

local GetMiningSpeedUpResInfoMessage = BaseClass("GetMiningSpeedUpResInfoMessage", SFSBaseMessage)
local base = SFSBaseMessage
local Localization = CS.GameEntry.Localization

local function OnCreate(self, activityId, queueId, goodsNum)
    base.OnCreate(self)
    self.sfsObj:PutInt("activityId", tostring(activityId))
    self.sfsObj:PutInt("queueId", tostring(queueId))
    self.sfsObj:PutInt("goodsNum", tostring(goodsNum))
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    local errCode =  t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode)
    else
        DataCenter.MiningManager:OnRecvMiningSpeedUpResInfo(t)
    end
end

GetMiningSpeedUpResInfoMessage.OnCreate = OnCreate
GetMiningSpeedUpResInfoMessage.HandleMessage = HandleMessage

return GetMiningSpeedUpResInfoMessage