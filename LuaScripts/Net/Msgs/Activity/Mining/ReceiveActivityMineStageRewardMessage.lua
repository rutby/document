---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by 1020.
--- DateTime: 2023/5/24 14:51
---

local ReceiveActivityMineStageReward = BaseClass("ReceiveActivityMineStageReward", SFSBaseMessage)
local base = SFSBaseMessage
local Localization = CS.GameEntry.Localization

local function OnCreate(self, activityId, type)
    base.OnCreate(self)
    self.sfsObj:PutInt("activityId", tonumber(activityId))
    self.sfsObj:PutInt("type", tonumber(type))
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    local errCode =  t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode)
    else
        DataCenter.MiningManager:ReceiveStageRewardHandle(t)
    end
end

ReceiveActivityMineStageReward.OnCreate = OnCreate
ReceiveActivityMineStageReward.HandleMessage = HandleMessage

return ReceiveActivityMineStageReward