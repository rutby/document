---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by 1.
--- DateTime: 2023/3/8 17:18
--- ClaimAlContributeBoxRewardMessage.lua

local ClaimAlContributeBoxRewardMessage = BaseClass("ClaimAlContributeBoxRewardMessage", SFSBaseMessage)
local base = SFSBaseMessage
local Localization = CS.GameEntry.Localization

local function OnCreate(self, activityId, boxIndex)
    base.OnCreate(self)
    self.sfsObj:PutUtfString("actId", tostring(activityId))
    self.sfsObj:PutInt("stage", tonumber(boxIndex))
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    local errCode =  t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode)
    else
        DataCenter.AlContributeManager:OnRecvClaimBoxResp(t)
    end
end

ClaimAlContributeBoxRewardMessage.OnCreate = OnCreate
ClaimAlContributeBoxRewardMessage.HandleMessage = HandleMessage

return ClaimAlContributeBoxRewardMessage