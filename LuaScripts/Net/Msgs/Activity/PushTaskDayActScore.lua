---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by zzl.
--- DateTime: 
--- 七日积分更新

local PushTaskDayActScore = BaseClass("PushTaskDayActScore", SFSBaseMessage)
local base = SFSBaseMessage

local function OnCreate(self)
    base.OnCreate(self)
end

local function HandleMessage(self, message)
    base.HandleMessage(self, message)
    if message["errorCode"] ~= nil then
        UIUtil.ShowTips(Localization:GetString(message["errorCode"]))
        return
    end
    DataCenter.ActivityListDataManager:UpdateDayActScore(message)
end

PushTaskDayActScore.OnCreate = OnCreate
PushTaskDayActScore.HandleMessage = HandleMessage

return PushTaskDayActScore