---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by zzl
--- DateTime: 
--- 指派出战成员信息
local DragonAssignPlayerInfoMessage = BaseClass("DragonAssignPlayerInfoMessage", SFSBaseMessage)
local base = SFSBaseMessage

local function OnCreate(self)
    base.OnCreate(self)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    local errCode =  t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode) 
    else
        DataCenter.ActDragonManager:HandleGetPlayerList(t)
    end
end

DragonAssignPlayerInfoMessage.OnCreate = OnCreate
DragonAssignPlayerInfoMessage.HandleMessage = HandleMessage

return DragonAssignPlayerInfoMessage