---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by 1.
--- DateTime: 2023/2/7 11:15
---

local SendContactGiftSearchMessage = BaseClass("SendContactGiftSearchMessage", SFSBaseMessage)
local base = SFSBaseMessage

local function OnCreate(self, server, name)
    base.OnCreate(self)
    self.sfsObj:PutInt("serverId", server)
    self.sfsObj:PutUtfString("searchName", name)

end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    local errCode =  t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode) 
    else
        EventManager:GetInstance():Broadcast(EventId.SendContactGiftSearchBack, t)
    end
end

SendContactGiftSearchMessage.OnCreate = OnCreate
SendContactGiftSearchMessage.HandleMessage = HandleMessage

return SendContactGiftSearchMessage