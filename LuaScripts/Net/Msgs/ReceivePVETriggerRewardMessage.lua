---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by 1.
--- DateTime: 2022/8/12 18:18
---ReceivePVETriggerRewardMessage

local ReceivePVETriggerRewardMessage = BaseClass("ReceivePVETriggerRewardMessage", SFSBaseMessage)
local base = SFSBaseMessage

function ReceivePVETriggerRewardMessage:OnCreate(param)
    base.OnCreate(self)
    if param ~= nil then
        self.sfsObj:PutInt("level", param.level)
        self.sfsObj:PutInt("trigger", param.trigger)
        self.sfsObj:PutBool("costStamina", param.costStamina)
        if param.x ~= nil and param.y ~= nil then
            self.sfsObj:PutFloat("x", param.x)
            self.sfsObj:PutFloat("y", param.y)
        end
    end
end

function ReceivePVETriggerRewardMessage:HandleMessage(t)
    base.HandleMessage(self, t)
    local battleLevel = DataCenter.BattleLevel
    if battleLevel then
        battleLevel:OnReceivePveTriggerRewardHandler(t)
    end
end

return ReceivePVETriggerRewardMessage