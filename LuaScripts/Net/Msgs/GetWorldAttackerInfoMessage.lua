---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Imaginaerum.
--- DateTime: 2023/3/9 15:04
---
local GetWorldAttackerInfoMessage = BaseClass("GetWorldAttackerInfoMessage", SFSBaseMessage)
local base = SFSBaseMessage
local Localization = CS.GameEntry.Localization
local function OnCreate(self,serverId)
    base.OnCreate(self)
    self.sfsObj:PutInt("targetServer",serverId)
    self.sfsObj:PutInt("worldId", LuaEntry.Player:GetCurWorldId())
end
local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    local errCode =  t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode)
    else
        DataCenter.WorldNewsDataManager:UpdateAttackerInfoPointMsg(t)
        EventManager:GetInstance():Broadcast(EventId.AttackInfoForMinimap)
    end
end

GetWorldAttackerInfoMessage.OnCreate = OnCreate
GetWorldAttackerInfoMessage.HandleMessage = HandleMessage

return GetWorldAttackerInfoMessage