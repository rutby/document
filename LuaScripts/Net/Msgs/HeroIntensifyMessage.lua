---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Beef.
--- DateTime: 2023/2/15 15:22
---

local HeroIntensifyMessage = BaseClass("HeroIntensifyMessage", SFSBaseMessage)
local base = SFSBaseMessage

local function OnCreate(self, id, costType, heroUuid)
    base.OnCreate(self)
    self.sfsObj:PutInt("intensifyId", id)
    self.sfsObj:PutInt("costType", costType)
    if heroUuid then
        self.sfsObj:PutLong("costHeroUuid", heroUuid)
    end
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    if t["errorCode"] ~= nil then
        return
    end
    
    DataCenter.HeroIntensifyManager:HandleIntensify(t)
end

HeroIntensifyMessage.OnCreate = OnCreate
HeroIntensifyMessage.HandleMessage = HandleMessage

return HeroIntensifyMessage