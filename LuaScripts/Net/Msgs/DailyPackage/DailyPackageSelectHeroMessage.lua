---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by admin.
--- DateTime: 2023/1/5 20:34
---
local DailyPackageSelectHeroMessage = BaseClass("DailyPackageSelectHeroMessage", SFSBaseMessage)
local base = SFSBaseMessage
local Localization = CS.GameEntry.Localization

local function OnCreate(self, id)
    base.OnCreate(self)
    self.sfsObj:PutUtfString("id", id)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    local errCode =  t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode) 
    else
        local id = tonumber(t["selectId"])
        DataCenter.DailyPackageManager:OnChooseUpdate(t)
        EventManager:GetInstance():Broadcast(EventId.DailyPackageSelectHero, id)
    end
end

DailyPackageSelectHeroMessage.OnCreate = OnCreate
DailyPackageSelectHeroMessage.HandleMessage = HandleMessage

return DailyPackageSelectHeroMessage