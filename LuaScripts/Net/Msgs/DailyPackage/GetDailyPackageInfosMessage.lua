---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by admin.
--- DateTime: 2023/1/5 20:34
---
local GetDailyPackageInfosMessage = BaseClass("GetDailyPackageInfosMessage", SFSBaseMessage)
local base = SFSBaseMessage
local Localization = CS.GameEntry.Localization

local function OnCreate(self)
    base.OnCreate(self)
    
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    local errCode =  t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode) 
    else
        DataCenter.DailyPackageManager:UpdateDailyPackage(t)
        EventManager:GetInstance():Broadcast(EventId.GetDailyPackageInfos)
    end
end

GetDailyPackageInfosMessage.OnCreate = OnCreate
GetDailyPackageInfosMessage.HandleMessage = HandleMessage

return GetDailyPackageInfosMessage