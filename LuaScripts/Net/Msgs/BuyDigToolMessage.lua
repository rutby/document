---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by 1.
--- DateTime: 2022/10/20 18:08
---BuyDigToolMessage

local BuyDigToolMessage = BaseClass("BuyDigToolMessage", SFSBaseMessage)
local base = SFSBaseMessage
local Localization = CS.GameEntry.Localization

local function OnCreate(self, activityId, num)
    base.OnCreate(self)
    self.sfsObj:PutInt("activityId", activityId)
    self.sfsObj:PutInt("num", num)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    local errCode =  t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode)
    else
        if t["gold"]~=nil then
            LuaEntry.Player.gold = t["gold"]
            EventManager:GetInstance():Broadcast(EventId.UpdateGold)
        end
        DataCenter.DigActivityManager:OnRecvBuyItemRet(t)
    end
end

BuyDigToolMessage.OnCreate = OnCreate
BuyDigToolMessage.HandleMessage = HandleMessage

return BuyDigToolMessage