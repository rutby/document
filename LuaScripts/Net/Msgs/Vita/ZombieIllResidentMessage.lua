---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Beef.
--- DateTime: 2024/3/25 14:37
---

local ZombieIllResidentMessage = BaseClass("ZombieIllResidentMessage", SFSBaseMessage)
local base = SFSBaseMessage

local function OnCreate(self, uuids, type)
    base.OnCreate(self)

    local array = SFSArray.New()
    for _, uuid in ipairs(uuids) do
        array:AddLong(uuid)
    end
    self.sfsObj:PutSFSArray("uuidArr", array)
    self.sfsObj:PutInt("type", type)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)

    if t["errorCode"] == nil then
        DataCenter.VitaManager:HandleZombieAttack(t)
    end
end

ZombieIllResidentMessage.OnCreate = OnCreate
ZombieIllResidentMessage.HandleMessage = HandleMessage

return ZombieIllResidentMessage