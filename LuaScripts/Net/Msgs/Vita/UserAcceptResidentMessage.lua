---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Beef.
--- DateTime: 2023/11/9 15:15
---

local UserAcceptResidentMessage = BaseClass("UserAcceptResidentMessage", SFSBaseMessage)
local base = SFSBaseMessage

local function OnCreate(self, uuids)
    base.OnCreate(self)

    local array = SFSArray.New()
    for _, uuid in ipairs(uuids) do
        array:AddLong(uuid)
    end
    self.sfsObj:PutSFSArray("uuidList", array)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    if t["errorCode"] == nil then
        DataCenter.VitaManager:HandleAcceptResident(t)
    end
end

UserAcceptResidentMessage.OnCreate = OnCreate
UserAcceptResidentMessage.HandleMessage = HandleMessage

return UserAcceptResidentMessage