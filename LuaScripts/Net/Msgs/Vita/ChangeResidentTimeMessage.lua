---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Beef.
--- DateTime: 2023/11/28 11:43
---

local ChangeResidentTimeMessage = BaseClass("ChangeResidentTimeMessage", SFSBaseMessage)
local base = SFSBaseMessage

local function OnCreate(self, hour)
    base.OnCreate(self)
    self.sfsObj:PutInt("hour", hour)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)

    if t["errorCode"] == nil then
        DataCenter.VitaManager:HandleChangeTime(t)
    end
end

ChangeResidentTimeMessage.OnCreate = OnCreate
ChangeResidentTimeMessage.HandleMessage = HandleMessage

return ChangeResidentTimeMessage