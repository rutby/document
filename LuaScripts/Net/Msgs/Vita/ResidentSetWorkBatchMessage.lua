---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Beef.
--- DateTime: 2024/3/19 20:35
---

local ResidentSetWorkBatchMessage = BaseClass("ResidentSetWorkBatchMessage", SFSBaseMessage)
local base = SFSBaseMessage

local function OnCreate(self, dict)
    base.OnCreate(self)

    local array = SFSArray.New()
    for uuid, fUuid in pairs(dict) do
        local obj = SFSObject.New()
        obj:PutLong("uuid", uuid)
        obj:PutLong("fUuid", fUuid)
        array:AddSFSObject(obj)
    end
    self.sfsObj:PutSFSArray("changeArr", array)
    DataCenter.GuideManager:SetWaitingMessage(WaitMessageFinishType.ResidentSetWork, true)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    
    if t["errorCode"] == nil then
        DataCenter.VitaManager:HandleSetResidentWorkBatch(t)
    end
    DataCenter.GuideManager:SetWaitingMessage(WaitMessageFinishType.ResidentSetWork, nil)
    EventManager:GetInstance():Broadcast(EventId.GuideWaitMessage)
end

ResidentSetWorkBatchMessage.OnCreate = OnCreate
ResidentSetWorkBatchMessage.HandleMessage = HandleMessage

return ResidentSetWorkBatchMessage