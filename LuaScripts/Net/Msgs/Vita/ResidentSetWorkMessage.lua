---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Beef.
--- DateTime: 2023/11/3 11:24
---

local ResidentSetWorkMessage = BaseClass("ResidentSetWorkMessage", SFSBaseMessage)
local base = SFSBaseMessage

local function OnCreate(self, uuid, fUuid)
    base.OnCreate(self)
    
    self.sfsObj:PutLong("uuid", uuid)
    self.sfsObj:PutLong("fUuid", fUuid)
    DataCenter.GuideManager:SetWaitingMessage(WaitMessageFinishType.ResidentSetWork, true)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    
    if t["errorCode"] == nil then
        DataCenter.VitaManager:HandleSetResidentWork(t)
    end
    DataCenter.GuideManager:SetWaitingMessage(WaitMessageFinishType.ResidentSetWork, nil)
    EventManager:GetInstance():Broadcast(EventId.GuideWaitMessage)
end

ResidentSetWorkMessage.OnCreate = OnCreate
ResidentSetWorkMessage.HandleMessage = HandleMessage

return ResidentSetWorkMessage