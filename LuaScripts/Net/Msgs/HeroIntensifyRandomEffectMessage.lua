---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Beef.
--- DateTime: 2023/2/17 17:34
---

local HeroIntensifyRandomEffectMessage = BaseClass("HeroIntensifyRandomEffectMessage", SFSBaseMessage)
local base = SFSBaseMessage

local function OnCreate(self, id)
    base.OnCreate(self)
    self.sfsObj:PutInt("intensifyId", id)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    if t["errorCode"] ~= nil then
        return
    end
    
    DataCenter.HeroIntensifyManager:HandleIntensifyRandomEffect(t)
end

HeroIntensifyRandomEffectMessage.OnCreate = OnCreate
HeroIntensifyRandomEffectMessage.HandleMessage = HandleMessage

return HeroIntensifyRandomEffectMessage