---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by 1.
--- DateTime: 2023/1/9 14:29
---ClaimPaidLotteryTicketMessage


local ClaimPaidLotteryTicketMessage = BaseClass("ClaimPaidLotteryTicketMessage", SFSBaseMessage)
local base = SFSBaseMessage
local Localization = CS.GameEntry.Localization

local function OnCreate(self, activityId, stageId)
    base.OnCreate(self)
    self.sfsObj:PutInt("activityId", activityId)
    self.sfsObj:PutInt("stageId", stageId)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    local errCode =  t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode)
    else
        DataCenter.PaidLotteryManager:OnRecvClaimTicketsResp(t)
    end
end

ClaimPaidLotteryTicketMessage.OnCreate = OnCreate
ClaimPaidLotteryTicketMessage.HandleMessage = HandleMessage

return ClaimPaidLotteryTicketMessage
