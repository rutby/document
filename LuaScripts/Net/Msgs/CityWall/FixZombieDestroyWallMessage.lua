---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Beef.
--- DateTime: 2024/4/12 15:36
---

local FixZombieDestroyWallMessage = BaseClass("FixZombieDestroyWallMessage", SFSBaseMessage)
local base = SFSBaseMessage

local function OnCreate(self, wallIds)
    base.OnCreate(self)
    local array = SFSArray.New()
    for _, wallId in ipairs(wallIds) do
        array:AddInt(wallId)
    end
    self.sfsObj:PutSFSArray("wallIds", array)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)

    if t["errorCode"] == nil then
        DataCenter.CityWallManager:HandleRepairWall(t)
    end
end

FixZombieDestroyWallMessage.OnCreate = OnCreate
FixZombieDestroyWallMessage.HandleMessage = HandleMessage

return FixZombieDestroyWallMessage