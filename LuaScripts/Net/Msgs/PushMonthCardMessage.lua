---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by 1.
--- DateTime: 2021/11/4 10:27
---

local PushMonthCardMessage = BaseClass("PushMonthCardMessage", SFSBaseMessage)
local base = SFSBaseMessage
local Localization = CS.GameEntry.Localization
local function OnCreate(self)
    base.OnCreate(self)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    local errCode =  t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode) 
    else
        DataCenter.MonthCardNewManager:UpdateMonthCardData(t)
        DataCenter.AlContributeManager:UpdateMonthCardInfo(t)
		if t["lastUpdateTime"] then
			LuaEntry.Player:SetLastUpdateTime(t["lastUpdateTime"])
		end
        --月卡
        local mainBuild = DataCenter.BuildManager:GetFunbuildByItemID(BuildingTypes.FUN_BUILD_TRAINFIELD_4)
        if mainBuild then
            EventManager:GetInstance():Broadcast(EventId.UserCitySkinUpdate, mainBuild.uuid)
        end
    end
end

PushMonthCardMessage.OnCreate = OnCreate
PushMonthCardMessage.HandleMessage = HandleMessage

return PushMonthCardMessage