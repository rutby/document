---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Beef.
--- DateTime: 2023/7/25 10:54
---

local SendMarchEmojiMessage = BaseClass("SendMarchEmojiMessage", SFSBaseMessage)
local base = SFSBaseMessage

local function OnCreate(self, targetType, uuid, emo)
    self.sfsObj:PutInt("type", targetType)
    self.sfsObj:PutLong("uuid", uuid)
    self.sfsObj:PutUtfString("emoji", emo)
    base.OnCreate(self)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    WorldMarchEmotionManager:GetInstance():HandleSend(t)
end

SendMarchEmojiMessage.OnCreate = OnCreate
SendMarchEmojiMessage.HandleMessage = HandleMessage

return SendMarchEmojiMessage