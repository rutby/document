---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Imaginaerum.
--- DateTime: 2023/10/30 14:35
---
local CrossEdenMineServerRankMessage = BaseClass("CrossEdenMineServerRankMessage", SFSBaseMessage)
local base = SFSBaseMessage
local Localization = CS.GameEntry.Localization
local function OnCreate(self,server)
    base.OnCreate(self)
    self.sfsObj:PutInt("server",server)
end
local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    local errCode = t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode)
    else
        EventManager:GetInstance():Broadcast(EventId.GetEdenMineScoreRank, t)
    end
end

CrossEdenMineServerRankMessage.OnCreate = OnCreate
CrossEdenMineServerRankMessage.HandleMessage = HandleMessage

return CrossEdenMineServerRankMessage