---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by guqu.
--- DateTime: 2020/9/3 18:00
---
local PlayerKillRankListMessage = BaseClass("PlayerKillRankListMessage", SFSBaseMessage)
local base = SFSBaseMessage
local Localization = CS.GameEntry.Localization
local function OnCreate(self)
    base.OnCreate(self)
    self.sfsObj:PutInt("ismerge",0)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    local errCode =  t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode) 
    else
        DataCenter.RankDataManager:ParsePlayerRankData(t,RankType.CommanderKill)
        EventManager:GetInstance():Broadcast(EventId.PlayerRank)
    end
end

PlayerKillRankListMessage.OnCreate = OnCreate
PlayerKillRankListMessage.HandleMessage = HandleMessage

return PlayerKillRankListMessage