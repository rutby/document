---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Imaginaerum.
--- DateTime: 2023/2/13 15:59
---
local GetAlFlagDestroyTimeMessage = BaseClass("GetAlFlagDestroyTimeMessage", SFSBaseMessage)
-- 基类，用来调用基类方法
local base = SFSBaseMessage
local Localization = CS.GameEntry.Localization
local function OnCreate(self)
    base.OnCreate(self)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    local errCode =  t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode)
    else
        if t["fortress"]~=nil then
            DataCenter.AllianceMineManager:RefreshAllianceFlagTime(t["fortress"])
        end
        if t["timeArr"]~=nil then
            DataCenter.AllianceMineManager:RefreshAllianceFrontTime(t)
        end
        
    end
end

GetAlFlagDestroyTimeMessage.OnCreate = OnCreate
GetAlFlagDestroyTimeMessage.HandleMessage = HandleMessage

return GetAlFlagDestroyTimeMessage