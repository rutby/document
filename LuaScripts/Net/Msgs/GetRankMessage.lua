---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by.
--- DateTime:
--- 通用排行榜信息
local GetRankMessage = BaseClass("GetRankMessage", SFSBaseMessage)
local base = SFSBaseMessage
local function OnCreate(self,type)
	base.OnCreate(self)
	self.sfsObj:PutInt("type",type)
end

local function HandleMessage(self, t)
	base.HandleMessage(self, t)
	local errCode =  t["errorCode"]
	if errCode ~= nil then
		UIUtil.ShowTipsId(errCode)
	else
		EventManager:GetInstance():Broadcast(EventId.GetRankRefresh,t)
	end
end

GetRankMessage.OnCreate = OnCreate
GetRankMessage.HandleMessage = HandleMessage

return GetRankMessage