---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by
--- DateTim
---GetEdenUserRankMessage


local GetEdenUserRankMessage = BaseClass("GetEdenUserRankMessage", SFSBaseMessage)
local base = SFSBaseMessage
local Localization = CS.GameEntry.Localization

local function OnCreate(self,rankType)
    base.OnCreate(self)
    self.sfsObj:PutInt("rankType", rankType)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    local errCode =  t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode)
    else
        DataCenter.GloryManager:SetSFiveEdenUserRank(t)
    end
end

GetEdenUserRankMessage.OnCreate = OnCreate
GetEdenUserRankMessage.HandleMessage = HandleMessage

return GetEdenUserRankMessage
