---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by .
--- DateTime:
---
local GetDesertForceGroupRankMessage = BaseClass("GetDesertForceGroupRankMessage", SFSBaseMessage)
local base = SFSBaseMessage
local function OnCreate(self,activityId)
    base.OnCreate(self)
    self.sfsObj:PutInt("activityId",activityId)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    local errCode =  t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode)
    else
        EventManager:GetInstance():Broadcast(EventId.ActDesertForceGroupRank,t)
    end
end

GetDesertForceGroupRankMessage.OnCreate = OnCreate
GetDesertForceGroupRankMessage.HandleMessage = HandleMessage

return GetDesertForceGroupRankMessage