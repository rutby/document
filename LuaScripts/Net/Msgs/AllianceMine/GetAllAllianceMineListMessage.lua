---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by 1.
--- DateTime: 2022/12/9 8:44
---GetAllAllianceMineListMessage.lua


local GetAllAllianceMineListMessage = BaseClass("GetAllAllianceMineListMessage", SFSBaseMessage)
local base = SFSBaseMessage
local Localization = CS.GameEntry.Localization
local function OnCreate(self, needDetail)
    base.OnCreate(self)
    self.sfsObj:PutBool("needDetail", needDetail)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    local errCode =  t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode)
    else
        DataCenter.AllianceMineManager:UpdateAllianceMineInfo(t)
    end
end

GetAllAllianceMineListMessage.OnCreate = OnCreate
GetAllAllianceMineListMessage.HandleMessage = HandleMessage

return GetAllAllianceMineListMessage