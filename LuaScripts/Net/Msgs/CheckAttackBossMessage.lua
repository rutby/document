---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by 24224.
--- DateTime: 2022/12/14 10:10
---
local CheckAttackBossMessage = BaseClass("CheckAttackBossMessage", SFSBaseMessage)
local base = SFSBaseMessage
local Localization = CS.GameEntry.Localization

local function OnCreate(self,uuid)
    base.OnCreate(self)
    self.sfsObj:PutLong("uuid",uuid)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    local errCode =  t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode)
    else
        local hasMarch = t["hasMarch"]
        if hasMarch~=nil then
            if hasMarch == true then
                EventManager:GetInstance():Broadcast(EventId.CheckAttackBoss,1)
            else
                EventManager:GetInstance():Broadcast(EventId.CheckAttackBoss,0)
            end
        end
    end
end

CheckAttackBossMessage.OnCreate = OnCreate
CheckAttackBossMessage.HandleMessage = HandleMessage

return CheckAttackBossMessage