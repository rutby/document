---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by 1.
--- DateTime: 2022/5/16 19:00
---

local PushAllianceOfficialPosChangeMessage = BaseClass("PushAllianceOfficialPosChangeMessage", SFSBaseMessage)
local base = SFSBaseMessage
local Localization = CS.GameEntry.Localization

local function OnCreate(self)
    base.OnCreate(self)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    local errCode = t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode)
    else
        if t.newOfficial then
            local type = t.newOfficial.type
            local uid = t.newOfficial.uid
            local userName = t.newOfficial.userName
            UIUtil.ShowTips(Localization:GetString("391055", userName, Localization:GetString(AllianceOfficialPosConf[type].name)))
        end
        if t.allianceOfficialArr then
            DataCenter.AllianceMemberDataManager:UpdateAlliancePosition(t)
        end
    end
end
PushAllianceOfficialPosChangeMessage.OnCreate = OnCreate
PushAllianceOfficialPosChangeMessage.HandleMessage = HandleMessage

return PushAllianceOfficialPosChangeMessage