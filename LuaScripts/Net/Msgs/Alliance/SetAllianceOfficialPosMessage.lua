---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by 1.
--- DateTime: 2022/5/16 7:45
---al.set.offical


local SetAllianceOfficialPosMessage = BaseClass("SetAllianceOfficialPosMessage", SFSBaseMessage)
local base = SFSBaseMessage

local function OnCreate(self,uid, pos)
    base.OnCreate(self)
    self.sfsObj:PutInt("officalType", pos)
    self.sfsObj:PutUtfString("playerId", uid)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    local errCode = t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode)
    else
        local pos = t["officalType"]
        local uid = t["playerId"]
        DataCenter.AllianceMemberDataManager:UpdateOneAlliancePos(pos, uid)
    end
    
end
SetAllianceOfficialPosMessage.OnCreate = OnCreate
SetAllianceOfficialPosMessage.HandleMessage = HandleMessage

return SetAllianceOfficialPosMessage