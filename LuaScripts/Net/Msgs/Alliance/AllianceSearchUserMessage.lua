---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Imaginaerum.
--- DateTime: 2021/6/10 15:48
---
local AllianceSearchUserMessage = BaseClass("AllianceSearchUserMessage", SFSBaseMessage)
local base = SFSBaseMessage
local Localization = CS.GameEntry.Localization
local function OnCreate(self,key,lang,page,type)
    base.OnCreate(self)
    self.sfsObj:PutInt("type", type)
    self.sfsObj:PutInt("page", page)
    self.sfsObj:PutUtfString("key", key)
    self.sfsObj:PutUtfString("lang", lang)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    local errCode =  t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode) 
    else
        DataCenter.AllianceTempListManager:RefreshAllianceInviteList(t)
        EventManager:GetInstance():Broadcast(EventId.GET_INVITE_USERS_SUCCESS)
    end

end

AllianceSearchUserMessage.OnCreate = OnCreate
AllianceSearchUserMessage.HandleMessage = HandleMessage

return AllianceSearchUserMessage