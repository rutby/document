---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by 1.
--- DateTime: 2022/4/28 11:57
---


local AllianceInviteAcceptMessage = BaseClass("AllianceInviteAcceptMessage", SFSBaseMessage)
local base = SFSBaseMessage
local Localization = CS.GameEntry.Localization
local function OnCreate(self, mailUuid)
    base.OnCreate(self)
    self.sfsObj:PutUtfString("mailId",mailUuid)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    local errCode =  t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode)
    else
        local mailId = t["mailId"]
        local strKey = "MailInviteDeal_" .. mailId
        CS.GameEntry.Setting:SetInt(strKey, 2)
        EventManager:GetInstance():Broadcast(EventId.AllianceInviteStatusChange)
        UIUtil.ShowTipsId(391047)
    end
end

AllianceInviteAcceptMessage.OnCreate = OnCreate
AllianceInviteAcceptMessage.HandleMessage = HandleMessage

return AllianceInviteAcceptMessage