---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by 1.
--- DateTime: 2022/8/22 17:15
---PushAllianceTechRecommendMessage


local PushAllianceTechRecommendMessage = BaseClass("PushAllianceTechRecommendMessage", SFSBaseMessage)
local base = SFSBaseMessage
local Localization = CS.GameEntry.Localization
local function OnCreate(self)
    base.OnCreate(self)
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    local errCode =  t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode)
    else
        DataCenter.AllianceScienceDataManager:UpdateAllianceScienceRecommend(t)
    end
end
PushAllianceTechRecommendMessage.OnCreate = OnCreate
PushAllianceTechRecommendMessage.HandleMessage = HandleMessage

return PushAllianceTechRecommendMessage