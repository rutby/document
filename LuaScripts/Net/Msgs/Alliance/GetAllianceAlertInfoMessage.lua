---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by zzl.
--- DateTime: 
---
local GetAllianceAlertInfoMessage = BaseClass("GetAllianceAlertInfoMessage", SFSBaseMessage)
local base = SFSBaseMessage
local Localization = CS.GameEntry.Localization

local function OnCreate(self,targetServer)
    base.OnCreate(self)
    self.sfsObj:PutInt("targetServer",targetServer)     --敌对服
end

local function HandleMessage(self, t)
    base.HandleMessage(self, t)
    local errCode =  t["errorCode"]
    if errCode ~= nil then
        UIUtil.ShowTipsId(errCode)
    else
        DataCenter.AllianceAlertDataManager:InitAllianceAlertList(t)
        
        --EventManager:GetInstance():Broadcast(EventId.AllianceWarUpdate)
    end
end

GetAllianceAlertInfoMessage.OnCreate = OnCreate
GetAllianceAlertInfoMessage.HandleMessage = HandleMessage

return GetAllianceAlertInfoMessage